# Project State

- ES contract selection now uses the IBKR chain plus `config/session.toml` only.
- The date CSV and `ES_ROLL_MODE` environment setting are removed from the operational path.
- A read-only live IBKR chain query selected `ESU6`, conId `649180671`, at the current server time.
